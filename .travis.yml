language: node_js
node_js:
  - 12
  - 14
  - 16

install:
  - npm install
  - npm run link

script:
  - npm run lint
  - npm test

jobs:
  include:
    - stage: release
      node_js: lts/*
      script:
        - npm run release
